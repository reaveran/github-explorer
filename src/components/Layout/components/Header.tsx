import { Typography } from "@/components";

const Header = () => {
  return (
    <header className="mb-6">
      <Typography variant="h2">Github Explorer</Typography>
    </header>
  );
};

export default Header;
